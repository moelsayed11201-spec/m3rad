# دليل إعداد نظام تسجيل الدخول اليدوي (Firebase Email/Password Auth)

لقد قمنا باستبدال نظام تسجيل الدخول عبر Google بنظام تسجيل دخول يدوي آمن باستخدام البريد الإلكتروني وكلمة المرور عبر **Firebase Authentication**.

لتفعيل وتشغيل النظام بشكل كامل، يرجى اتباع الخطوات التالية في لوحة تحكم Firebase:

---

## 1. تفعيل موفر البريد الإلكتروني وكلمة المرور (Enable Email/Password Provider)
1. افتح [منصة Firebase](https://console.firebase.google.com/).
2. اختر مشروعك الخاص بالمنظومة.
3. من القائمة الجانبية، اذهب إلى **Build > Authentication**.
4. اضغط على التبويب **Sign-in method**.
5. انقر على **Add new provider** واختر **Email/Password**.
6. قم بتفعيل الخيار (Enable) ثم اضغط **Save** (احفظ).

---

## 2. إنشاء المستخدمين التجريبيين في Firebase Console
قم بإنشاء المستخدمين الثلاثة يدوياً من خلال تبويب **Users** في لوحة التحكم في Firebase:

| البريد الإلكتروني (Email) | الصلاحية الافتراضية (Role) | كلمة المرور التجريبية |
| :--- | :--- | :--- |
| `ceo@gmail.com` | المدير العام (CEO) | `134567` |
| `admin@gmail.com` | مدير فرع (Manager) | `134567` |
| `employee@gmail.com` | موظف (Employee) | `134567` |

*ملاحظة هامة: يجب نسخ المعرف الفريد (UID) الخاص بكل مستخدم بعد إنشائه لمطابقته في قاعدة البيانات Firestore.*

---

## 3. مطابقة المستخدمين في Firestore (تتم تلقائياً في أول دخول أو يدوياً)
لقد قمنا ببناء **نظام تهيئة تلقائي آمن (Safe Auto-Provisioning)** في تطبيق الويب.
بمجرد قيامك بتسجيل الدخول لأول مرة باستخدام أي من الحسابات الثلاثة السابقة بكلمة مرورها، سيقوم النظام تلقائياً بإنشاء مستند المستخدم الخاص بها في مسار `users/{UID}` بالصلاحيات الصحيحة والحالة النشطة!

إذا كنت ترغب في كتابتها يدوياً عبر Firestore Console، اذهب إلى **Firestore Database** وأنشئ مستنداً في مجموعة (`users`) بالمعرف الفريد (UID) للمستند بالبنية التالية:

```json
users/{UID}
{
  "id": "UID_الخاص_بالمستخدم",
  "email": "ceo@gmail.com",
  "name": "المدير العام / CEO",
  "role": "ceo",
  "isActive": true,
  "branchIds": [],
  "createdAt": "2026-05-23T06:15:00Z",
  "createdBy": "system"
}
```

---

## 4. تحذير أمني هام لبيئة الإنتاج (Production Security Guard)
1. **تغيير كلمات المرور الضعيفة**: كلمة المرور `134567` هي كلمة مرور تجريبية ضعيفة مخصصة للتطوير والمراجعة فقط. يجب إجبار المستخدمين على تغييرها فوراً قبل بدء الاستخدام الفعلي للنظام لتجنب الاختراقات.
2. **منع التسجيل العام**: تم حظر إمكانية إنشاء الحسابات للعامة من الواجهات الأمامية بالكامل. لا يمكن لأي مستخدم جديد الدخول إلا إذا قام المدير العام (CEO) بإضافته وتعيين فروع له من صفحة إدارة المستخدمين بالمنظومة.
